# Playwright Image Downloader Environment Configuration

# Unsplash API Access Key (Required)
# Get this from: https://unsplash.com/developers
UNSPLASH_ACCESS_KEY=your_access_key_here

# Unsplash User Credentials (Optional - for auto-login)
# These are optional but enable automatic login for better download access
# If not provided, you may need to login manually when running in non-headless mode
UNSPLASH_EMAIL=your_email@example.com
UNSPLASH_PASSWORD=your_password_here

# Download Configuration (Optional)
# These can also be overridden via command line arguments

# Timeout for each download in milliseconds (default: 30000)
PLAYWRIGHT_TIMEOUT=30000

# Number of retry attempts for failed downloads (default: 3)
PLAYWRIGHT_RETRIES=3

# Preferred image size (default: original)
# Options: original, large, medium, small
PLAYWRIGHT_PREFERRED_SIZE=original

# Run in headless mode by default (default: true)
PLAYWRIGHT_HEADLESS=true

# Enable debug mode (default: false)
PLAYWRIGHT_DEBUG=false

# Limit the number of images to download (default: 0 = no limit)
PLAYWRIGHT_LIMIT=0
