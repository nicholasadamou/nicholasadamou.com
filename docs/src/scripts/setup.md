# Setup Scripts

Environment validation and configuration scripts.

No setup scripts are currently required. VSCO images are served from a local data export
in `data/vsco-export.json` and `public/images/vsco/` — no external tooling needed.
