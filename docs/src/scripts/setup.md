# Setup Scripts

Environment validation and configuration scripts.

No setup scripts are currently required. VSCO images are served from VSCO's CDN using
metadata in `data/vsco-export.json` — no external tooling needed.
